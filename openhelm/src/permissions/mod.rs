// Permission gate constants — kept as a module for future expansion
// (e.g. Network, Exec). Currently the profile's [permissions] table
// drives enablement directly via ProfilePermissions in config.
